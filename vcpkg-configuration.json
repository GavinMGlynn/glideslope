{
  "$comment": "Cesium Native's own port of KTX, this project's stb (cmake/ports/stb/portfile.cmake says why), and this project's triplets. The other ports are the vcpkg checkout's, at the commit cmake/Vcpkg.cmake pins.",
  "overlay-ports": [
    "./ext/cesium-native/extern/vcpkg/ports",
    "./cmake/ports"
  ],
  "overlay-triplets": [
    "./cmake/triplets"
  ]
}
